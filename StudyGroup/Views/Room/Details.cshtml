@model StudyGroup.Models.Room

@{
    ViewBag.Title = "Details";
}

<h2><font color="#b7a57a">Details</font></h2>

<div>
    <h4><font color="#b7a57a">Room</font></h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            <font color="white">@Html.DisplayNameFor(model => model.RoomName)</font>
</dt>

        <dd>
            <font color="white"> @Html.DisplayFor(model => model.RoomName)</font>
</dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.RoomID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
